module.exports = {
  appDirectory: "app", // Ensure this points to your app folder
  assetsBuildDirectory: "public/build",
  publicPath: "/build/",
  serverBuildPath: "build/index.js",
  ignoredRouteFiles: ["**/.*"], // Ensure no valid routes are excluded
};
